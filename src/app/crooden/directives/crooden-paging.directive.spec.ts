import { CroodenPagingDirective } from './crooden-paging.directive';

describe('CroodenPagingDirective', () => {
  it('should create an instance', () => {
    const directive = new CroodenPagingDirective();
    expect(directive).toBeTruthy();
  });
});
