import { CroodenGridDirective } from './crooden-grid.directive';

describe('CroodenGridDirective', () => {
  it('should create an instance', () => {
    const directive = new CroodenGridDirective();
    expect(directive).toBeTruthy();
  });
});
